# Stage C Round 1 plan — Strategy 1a (em-dash + en-dash strip)

## Strategy

1a = strip em-dashes (—) and en-dashes (–) outside page-range citations.

## Pre-edit audit

Body of MAUSOLEO_FULL_DRAFT_v10.md (lines 1-171, before References):
- em-dashes (—): **0**
- en-dashes (–): **0**

References (lines 172+) contain en-dashes inside page-ranges (e.g. "490–509"). These are LEGITIMATE per essay-iter SKILL ("outside page-range citations") and MUST be preserved.

## Verdict on Strategy 1a

**No-op for R95 baseline.** The dash scrub was completed earlier in Stage A/B (rounds R57/R67 lessons applied during build). Nothing to edit.

## Action

- Do NOT edit the file.
- Do NOT consume a GPTZero / GAN quota for a no-op (per dispatch rule 2 "PER ROUND budget is one GPTZero scan + one GAN check").
- Mark Strategy 1a as complete (zero-cost confirmation).
- Advance immediately to R2_C = Strategy 1b on next round.

## Pareto

GPTZero baseline: 0.929 (R95 unchanged).
GAN baseline: 2/3 PASS LEAN (R95 unchanged).
